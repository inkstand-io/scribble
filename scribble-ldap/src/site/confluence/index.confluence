h1. ${pageTitle}

${project.summary}

h2. Dependency Info

{code}
    <dependency>
      <groupId>io.inkstand</groupId>
      <artifactId>${artifactId}</artifactId>
      <version>${version}</version>
    </dependency>
{code}
